class AStar {
    /**
     * @param {number[][]} grid
     * @param {object} startPos
     * @param {object} endPos
    */
    constructor(grid, startPos, endPos) {
        this.grid = grid;
        this.startPos = startPos;
        this.endPos = endPos;
    }

    step() {

    }
}